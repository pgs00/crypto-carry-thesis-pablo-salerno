# Financial domain task

Read this first; these are binding interfaces. Work ONLY in forecast.py, costs.py, ledger.py, margin.py, portfolio.py and tests/unit/test_finance.py. Do not modify models.py/config.py. No subagents. No git commits (root integrates). Report to docs/tasks/finance_report.md.

Read prompt sections 5,8,9,13 in docs/sources/Prompt_Codex_Backtesting.md. Code/comments/tests English. All money/quantity Decimal. Use Config/Trade/Funding/Mark/MarketRule/Tier/Fill in existing models.py and config.py. Write failing financial tests first and run, then implement. pytest is installed via uv. Use .venv/Scripts/python.exe -m pytest to avoid uv cache races. Finance is independent of Nautilus, strategy and data loaders.

## Required public interface

- forecast.py: Forecast dataclass with value Decimal, no_change Decimal, anchor int, available_at int, history_start int, valid bool, reason str. `forecast(history: list[Funding], anchor: int, decision_time: int, config: Config) -> Forecast`. Require complete duration coverage (oldest antecedent covering window), interval_verified, positive actual intervals, each rate available and contiguous; exclude exact lower boundary from weighted sum, retain antecedent. Half-life duration arithmetic, forecast normalized weighted sum interval hours. `weight(age_hours, half_life_hours) -> Decimal`.
- costs.py: `floor_step(value: Decimal, step: Decimal) -> Decimal`; `execution_price(reference: Decimal, side: str, rule: MarketRule, config: Config) -> tuple[Decimal, Decimal]` returns adverse slippage price rounded up buys/down sells and separate rounding amount; `cycle_cost(spot_rule, futures_rule, config) -> Decimal` doubles both fees times cost_multiplier, adds 4*slippage*cost_multiplier. `valid_quantity(quantity, price, rule) -> bool`. Boundaries inclusive.
- margin.py: `maintenance(q, mark, rule) -> Decimal`; `liquidation_price(q, average, collateral, rule) -> Decimal | None` solve all tiers and reject lack of applicable evidence via ValueError, q0 returns None. `margin_state(q, average, collateral, mark, rule, config) -> dict` keys balance, maintenance, liquidation_price, ratio, distance, liquidate, preventive. Equality liquidates, no invalid divisions.
- ledger.py: `Position` dataclass fields spot, short, average, collateral, spot_cost, realized_spot, realized_futures, funding, fees, liquidation_fees, slippage; all Decimal default0. `Ledger(config: Config, strategy: str)` has free_spot (initial capital), free_futures (0), debt(0), positions dict symbol->Position, rows list[dict], funding_rows list[dict], seen set. `equity(spot_prices:dict, marks:dict)->Decimal`, `apply_fill(fill:Fill, rule:MarketRule, mark:Decimal)->bool`, `apply_funding(funding:Funding)->bool`, `transfer(amount:Decimal, time_ns:int, event_id:str)->bool` spot->futures only amount positive, `reconcile(spot_prices, marks)->dict`. Reject unaffordable voluntary increases without negative balances; fees/collateral conservatively checked before movement. Outgoing realized obligations Futures cash then spot, after release applicable collateral; residual debt. Funding negative waterfall Futures cash then same contract collateral then spot then debt. Incoming flows repay debt first. Funding/fills dedup by ID with conflicting duplicate rejected. Spot purchase fee in base units only, spot sale/Futures fees USDT. Isolated collateral q*fill/leverage + open loss, partial release proportionally. Liquidation fee basis execution_notional or mark_notional, ordinary fee per rule; do not repeat liquidation charge on retries. Persist rows include event_id,time_ns,strategy,symbol,kind, amount_usdt, money account balances, spot/short snapshots; explicit transfers/funding/fee/collateral/PnL as appropriate; no double counting. Ledger assert accounting movement identity after every fill/funding/transfer using exact reference valuation at fill/mark and scalar flow expectations (tolerance config), fail visibly, no corrective plugs. `reconcile` return equity, pnl, difference and verify capital+attributed total PnL identity. Spot average cost net needs fees attribution reconcile.
- portfolio.py: `target_quantity(equity, price, existing_spot, rule, config)->Decimal` returns total target spot gross units floor (include existing in net incremental cost separately), `needs_rebalance(spot_value, equity, config)->bool`, `hedge_error(spot, short)->Decimal` qspot0 shortpositive -> Infinity, both0 ->0; `required_cash(quantity, spot_price, futures_price, mark, spot_rule, futures_rule, config)->Decimal` forecast required spot purchase incl adverse fill and future fee/collateral/open loss (no future ticks).

Keep public API exact; tell root if needed adjustments. All baseline tests numerical cases in prompt financial paragraphs incl constant hourly 0.00001 ->0.00168 over168h, weights1,.5,.25, cost.0034, netspot9.99/cash9000 for 10@100 fee.001, q2mark100funding.001=.2, transfer equity unchanged, liq150/1.005, gaps block and dedup. Include debt, insolvency, partial collateral, tier-crossing, pricing PnL0/-1.
