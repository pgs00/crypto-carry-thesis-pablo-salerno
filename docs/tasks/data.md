# Bounded data pipeline task

Read first: implement ONLY src/crypto_carry/data/ (download.py, normalize.py, validate.py, rules.py, replay.py, __init__.py), tests/unit/test_data.py, docs/data_dictionary.md, docs/tasks/data_report.md. No subagents or commits. Root owns config/models/events/engine/CLI. Inspect those public models first. Read prompt section4 and data requirements in docs/sources/Prompt_Codex_Backtesting.md. Write meaningful offline tests first, run red, implement, run green. Use .venv/Scripts/python.exe -m pytest (uv env ready).

## Decisions and interface contracts

Config fields already defined. Root is Path cwd. All project paths stay inside root. Budget config.data_budget_bytes=20_000_000_000 is aggregate files in data/ including raw+processed; reserve/check prospective bytes and incremental writes, never overwrite raw or silently delete. Download starts sample_start 2024-01-01 to sample_end 2024-01-02, keeps baseline start2022/end2026 in base config. Data budget chosen by user, D drive available for future expansion but not used yet. VIP0 fixed withoutBNB/referrals, documented general promotions only. Never fabricate historical rules or replace settlement marks.

Public APIs:
- `download.download(config: Config, root: Path, scope: str='sample') -> dict`: scope sample default, full optional honors20GB hardcap. Download raw public funding (API paginated all history_start/end plus preceding antecedent), day trades spot/futures BTC/ETH for sample dates, markPriceKlines1m for sample. Public DataVision primary ZIP plus SHA256 CHECKSUM downloaded/verified zip integrity, resumed .part HTTPRange with validation, cache SHA check, bounded retry/backoff/rate limiting and manifest. All streams use httpx. Use official timestamps schemas; individual trades, no aggTrades substitution. Public API funding markPrice is empty on BTC2022 (already verified); preserve None and mark incomplete. DataVision fundingRate archive contains reported interval, download small months to cross-check funding schedule including missing-rate detection. Can restrict funding to sample warmup+horizon for initialdownload if fullfunding unnecessary but manifest exactrange. Store serialized descriptor manifest data/manifests/download.json including source_url,path,sha256,bytes,dataset,symbol,market,start,end,status,errors,retrieved_at. Repeated command never corrupts/duplicates. Return manifest. Download live bounded sample if feasible after tests, keeping all filesunderdata and no more20GB. Record actual observations.
- `normalize.normalize(config, root)->dict`: consume downloadmanifest, stream ZIPcsv chunks with exact price/qtydecimal text + integerns, writes Parquet under data/processed/dataset=X/symbol=X/market=X/date=YYYY-MM-DD/part-N.parquet. Do not materialize years of trades. Domain dataclasses fieldnames exact. Distinguish spot milliseconds before2025/microseconds since2025; Futures/API milliseconds. Mark open_time,close_time fromsource ns; available_at=close_time+1ms, never open-time information. Funding available_at=funding_time+60sec default (configdelay enacted engine separately), settlementmarkNone ifempty, interval_hours actualprevtime, interval_verified only authoritative calendar/archive corroboration. Duplicate identical removewithcounts; conflicting duplicates failpartition; positivevalues andtimes. Check budget during writes. Store processedmanifest paths/hash/rowcounts/types/ranges/schemas/dedupconflicts; normalized parquetfiles only replacewithidenticalorunique version, no unnoticed overwrites.
- `validate.validate_data(config,root, scope='sample')->dict`: produces data/manifests/coverage.json + data/manifests/data_coverage.csv + data/manifests/data_quality_report.md, dict status complete/incomplete_data, issues list[str], coverage list[dict], start/end exact, historical true. Validate actual requested samples/ranges incl fundingwarmup, funding missing rate/calendar/settlementmark, individualtrades continuity via trade_id acrossday (do not infer gaps from inactivity alone), filecompletehash evidence, 1minmarks coverage, rules. Missingrules means incomplete_data. Report fullbaselinecoverage separately; no forcecomplete from sampleonly. Do NOT call all-no-trade validifmissingfile. No invented returns.
- `rules.RuleBook(records: list[dict], allow_synthetic: bool=False)`: each record complete MarketRule snapshot under 'values' dictionary and fields symbol,market,rule_type='market',valid_from,valid_to,known_from,source_url,source_publication_time,retrieved_at,evidence_status ('verified','synthetic','unverified','current_snapshot'). Times ISOorintUTCns. `.get(symbol,market,time_ns)->MarketRule|None` requires validandknown, verified onlyorallowedsynthetic. Conflict overlaps fail, fulltypedrequiredmetadata; no currentexchangeinfo pastapply. `.load(path,allow_synthetic=False)` . `.transition_times(start,end)->list[int]` for known/valid boundaries. `synthetic_rules(config)->RuleBook` creates illustrative fullyexplicit synthetic rules onlyfixtures/demo, fee.001/.0005,steps.001/tick.01,tiers small andlargetestappropriate, cap mustallowAUM1million fordemo. Never usedinhistoricaldefault. data/rules/history.json initialized empty records with explanatory docs ifnoexacthistory; user specallows softwarewithoutverifiedrules. DO research and save official dated public evidence where accessible, but do not falsely certify. Feeprofileapprovednotconstantillustrativefees.
- `replay.iter_records(root:Path,start:int,end:int,warmup_hours:int=336)`: incremental chronological merge processedparquet perdataset/symbol, yields Trade/Funding/Mark/EventGap instances sortedphysicaltimestamp(seeevents.py), preservingcomplete sametimestamps forgroup_events includingpartitionedges, includefundingwarmup+priorobservationandlasttrade/markbeforestart ifexists. No artificialticks. UseArrowbatches andheapqmerge, avoidopenallthousandsfiles concurrently bypartitionchronology. `replay.input_hashes(root)->dict` pathsha256 processedmanifests plusrules, usablemanifest.

Tests: spot2025 timestampunit, futures2025stillms, conflicting/identicalduplicate rows, boundedbudgetreject withoutdownload, paginationadvance/nooverlap viafakeHTTP, rawchecksumcache, completevsunknowncoverage, RuleBookknown_from/validboundaries/current_snapshot notpast, gapnotintervalchange. Data schema must be align to models. Englishcode Spanishdocs. Return concise testevidence+interfaces+realdownloadstatus; report detailsdata_report.md.
