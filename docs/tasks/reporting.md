# Reporting task — read first

Implement ONLY src/crypto_carry/reporting.py, tests/unit/test_reporting.py, docs/tasks/reporting_report.md. No subagents, no commits. Root owns CLI/demo/robustness and existing evaluation.py. Read that module and actual Backtest public attributes before coding; exact finance rules are docs/sources/Prompt_Codex_Backtesting.md sections9-12. Code English, generated report Spanish. Write/run failing tests then implement and verify.

Public API: `write_run(root:Path, config:Config, backtests:list[Backtest], quality:dict, data_kind:str, label:str='baseline', inputs:dict|None=None)->Path` creates immutable outputs/<stable_run_id>/ and returns run directory. `regenerate_report(run_dir:Path)->Path` rebuilds report/figures ONLY from stored tables. Respect no overwrite: identical content can be verified/reused; changed report save new version or fail visibly. `verify_run(run_dir)->dict` rehash outputs and report mismatches; no unverified success.

Data structures: Backtest has config,strategy,funding_filter_enabled,status,reasons,stopped_at,now,ledger.positions (Position dataclass), ledger.rows,funding_rows,equity(),spot_prices(),mark_prices(), orders dict & order_rows list, fills list,signals list,positions list,risk_events list,daily list,opportunities list,all_funding list[Funding], durations dict seconds Decimal, native_fill_count/native_reconciliation_count. Financial rows contain Decimal, serialize exact money as decimal strings in Parquet. Config.to_dict/to_toml/digest; iso(ns) UTCns string. See evaluation.py `metric_tables`, `forecast_evaluation`, `h1_summary`, `daily_opportunity`,`regime_comparison` already implemented. Do not duplicate economic formulas beyond reporting attribution and never double-subtract slippage.

Quality dict generic status,issues,coverage list[dict],start/end; historical backtests=[] allowed when requireddata/rules unavailable. MUST generate incomplete_data report with NO invented flat-equity return. data_kind synthetic/historical explicit both manifest/report/figures. Synthetic never described academic historicalresults. Root CLI will call with emptybacktests for strict historical blockers.

Output files required: run_manifest.json (run_id,config,Python+dependencies,codehash,inputhashes,requested/observedrange,datakind,status,conventions,outputhashes,creationoperationaltime), effective_config.toml, data_quality_report.md,data_coverage.csv,signals.parquet,orders.parquet,fills.parquet,ledger.parquet,funding_payments.parquet,positions.parquet,risk_events.parquet,equity_daily.csv,metrics.csv,forecast_evaluation.csv,opportunity_daily.csv,regime_comparison.csv,robustness_summary.csv, figures/{equity,drawdown,pnl_components,forecast,opportunity,cost_sensitivity}.{png,svg}, report.md. Add h1_summary.csv,pnl_components.csv,execution_summary.csv,attribution_by_asset.csv if useful requirements. Root robust module writes actualscenario comparisons separately; initial robustness_summary row baseline label/status/metrics (no fake sensitivity results). Costplot initially label 'Sin escenarios de costos evaluados' if only baseline, not fabricate.

Stable run ID uses config+actual code hash+inputhashes+strategy/filterflags+data_kind+label; no createdtime inidentity. Never overwrite previous run; repeat same inputs run must validate artifacthashes thenreuse ID. Failed partial output gets failedmanifest, no falsecomplete. Reject rootrunid pathtraversal whenregenerate? CLI handles but functionsusePathresolvedreasonable. Every table includes run_id,symbol,strategy whereapplicable,timestamp_utc (fromtime_nsifavailable),units. Emptytable explicit schema and headers. Table exactdata/state goes tofiles; figures/report read savedfiles only. SHA256code includes src/**/*.py +pyproject+lock, rawsampleinputsmanifest fromroot passed inputs. Don't hash outputs into codehash. Sourcehash shouldfindprojectfor tmp_path testroot ifneeded.

Report must show real coverage, datakind/status, whatimplemented, timeframe/frequencies, assumptions2legsfullfills/1mMark/USDTpar/zeroRF/noADL/book/taxes/exchangeinsolvency, H1/H2/H3 descriptive judgments onlyvalidmetrics/coverage, dailycalendar/year/regimes, costs/slippageinfo, finalpositions/dust/debt/insolvency, executionfailure/riskcounts, exposed/unhedged/cash/cooldowntime, liquidityparticipationquantiles; AUMsensitivity notrun markedpending. Explain overlappingH1 horizons and no causality. Allnumbersfromsavedtables. For synthetic say these validate pipeline ONLY.

P&Lcomponents perasset: realizedspot + spot*lastspot -spot_cost, realizedfutures+short*(average-mark), funding, negativefees, negativeliquidationfees. Sum components equals equity-capital; slippage informational separate row/column NOT includedinthatsum. Nativepositioncheckcorrectenginecostfeesdifferentbaseinventory adjustment documented byrootmethodology.

Graphs staticpublicationexport Matplotlib Agg, decentSpanish labels, datesunitslegends, PNG+SVG, dimensionsreadable, no cutoffs. Figures fromsavedtables only. Determinism set SVGhashsalt runid, omit SVGDate metadata and stablePNGmetadata. Emptyhistorical diagnosticfigure honestly 'Sin observaciones verificadas'. Unit tests tinyfixturecanuseactualBacktest pernativefixture? prefer run corefast nofakeengine aggregate if using SimpleNamespace with faithfuldicts only reporter tests. Testfilesallpresent, moneystringroundtrip, syntheticlabels, emptyhistoricalnoequity, stableID/immutablecorruptiondetect, sumattributionreconcile. Print concise finalstatus and test/artifactevidence. Detailedreport docs/tasks/reporting_report.md.
